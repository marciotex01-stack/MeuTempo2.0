name: EstudoRecompensa
description: "Estuda → Ganha Tempo de Jogo - Monitoramento Real"
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  usage_stats: ^1.2.0
  device_apps: ^2.2.0
  workmanager: ^0.5.2
  permission_handler: ^11.3.1

flutter:
  uses-material-design: true
